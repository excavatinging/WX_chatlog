# Steps 1-2: stdlib only. Below are for step 3 / selftest / anchor locator.
pycryptodome>=3.20
# find_kdf_anchor.py (only needed when adapting to a new WeChat version)
capstone>=5.0
# Query/export layer (external project): pip install -e <wechat-cli source>
