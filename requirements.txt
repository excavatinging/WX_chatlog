# Steps 1-4 use stdlib only. Below are for offline selftest / version adaptation.
pycryptodome==3.23.0
# find_kdf_anchor.py (only needed when adapting to a new WeChat version)
capstone==5.0.9
# Optional downstream query layer is installed separately: python -m pip install wechat-cli
